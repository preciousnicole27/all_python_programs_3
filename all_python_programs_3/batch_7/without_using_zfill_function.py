# define the function
def zero_justified_text(text,width):
# check text length
    if len(text) >= width:
# return text
        return text
# add zeroes
# ask for user input
text = input("Enter a text:")
# print the results